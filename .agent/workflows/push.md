---
description: Changes staged, committed with a Korean message, and pushed to the repository.
---
1. Run `git status` to see what changes are pending.
2. Run `git add .` to stage all changes.
// turbo
3. Analyze the staged changes and generate a concise commit message in **Korean** (Hangul) that summarizes the work done.
4. Run `git commit -m "YOUR_KOREAN_COMMIT_MESSAGE"`.
5. Run `git push`.
